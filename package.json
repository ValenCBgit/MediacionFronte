{
  "name": "mediacion",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "lint": "biome ci .",
    "typecheck": "tsc -b",
    "vercel-build": "pnpm --filter @mediacion/shared build && pnpm --dir mediacion-app exec expo export --platform web --output-dir ../public && node scripts/fix-public-assets.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "typescript": "^7.0.2"
  }
}
